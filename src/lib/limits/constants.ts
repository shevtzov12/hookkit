/** Free tier: webhook events per inbox+IP per day. */
export const WEBHOOK_EVENTS_PER_DAY = 100;

/** Free tier: form submissions per form+IP per month. */
export const FORM_SUBMISSIONS_PER_MONTH = 500;

/** Outbound replay request timeout (ms). */
export const REPLAY_TIMEOUT_MS = 30_000;
